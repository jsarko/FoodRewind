version: '3'

services:
  web:
    build: .
    command: sh -c "python manage.py makemigrations && python manage.py migrate && python manage.py runserver 0.0.0.0:8000"
    container_name: food_rewind
    volumes:
      - db_data:/food_rewind/data
    ports:
      - "8000:8000"

volumes:
  db_data: